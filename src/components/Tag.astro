---
import type { Tag } from '../content/card_types';

interface Props {
    tagData: Tag
}

const { tagData } = Astro.props;
---

<div class="flex">
    <i class={"nf " + tagData.icon}></i>
    <p>{tagData.display}</p>
</div>